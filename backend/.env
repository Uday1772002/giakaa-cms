# MongoDB connection string - swap this with your atlas URI or local mongo
MONGODB_URI=mongodb://localhost:27017/giakaa_cms
PORT=5001

# just a simple flag for now, no auth needed per assignment spec
NODE_ENV=development
